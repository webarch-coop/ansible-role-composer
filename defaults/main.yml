---
# If php is php8 or something else set the composer_php variable
composer_php: php
# Specify a tag from https://github.com/composer/composer/releases
# or "latest" for the latest release
composer_version: latest
...
